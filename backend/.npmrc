# Omit devDependencies on server installs (npm ci / npm install in production).
# This skips vitest → rolldown → @emnapi transitive chain that causes lockfile
# version mismatches between different npm versions on CI/deploy hosts.
omit=dev
